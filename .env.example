# Kiwi Voice — secrets and API keys
# Loaded automatically at startup via python-dotenv
# Copy to .env and fill in your values

# RunPod TTS (provider=qwen3, qwen_backend=runpod)
RUNPOD_TTS_ENDPOINT_ID=your_endpoint_id
RUNPOD_API_KEY=your_runpod_api_key

# ElevenLabs (provider=elevenlabs)
KIWI_ELEVENLABS_API_KEY=your_elevenlabs_api_key

# Telegram approval (voice security)
KIWI_TELEGRAM_BOT_TOKEN=your_bot_token
KIWI_TELEGRAM_CHAT_ID=your_chat_id

# FFmpeg path (directory containing ffmpeg binary)
# KIWI_FFMPEG_PATH=/usr/local/bin

# Debug startup log (set to any value to enable)
# KIWI_DEBUG=1

# Optional overrides (uncomment to use):
# KIWI_TTS_PROVIDER=elevenlabs       # elevenlabs | piper | qwen3
# KIWI_QWEN_BACKEND=local            # runpod | local
# LLM_MODEL=openai/gpt-5.2
