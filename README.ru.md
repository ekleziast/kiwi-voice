<p align="center">
  <img src="https://em-content.zobj.net/source/apple/391/kiwi-fruit_1f95d.png" width="120" alt="Kiwi Voice">
</p>

<h1 align="center">Kiwi Voice</h1>

<p align="center">
  <strong>Голосовой ассистент для OpenClaw — от микрофона до ответа за секунды</strong>
</p>

<p align="center">
  <a href="https://github.com/ekleziast/kiwi-voice/blob/main/LICENSE"><img src="https://img.shields.io/badge/license-MIT-blue.svg" alt="License: MIT"></a>
  <a href="https://www.python.org/downloads/"><img src="https://img.shields.io/badge/python-3.10%2B-blue.svg" alt="Python 3.10+"></a>
  <a href="https://github.com/openclaw/openclaw"><img src="https://img.shields.io/badge/backend-OpenClaw-orange.svg" alt="OpenClaw"></a>
</p>

<p align="center">
  <a href="README.md">🇬🇧 English documentation</a>
</p>

---

## Что это?

Kiwi Voice превращает [OpenClaw](https://github.com/openclaw/openclaw) в голосового ассистента, работающего без рук. Слушает микрофон, распознаёт речь локально (Faster Whisper), понимает *кто* говорит, проверяет права доступа, отправляет запрос в LLM через WebSocket и озвучивает ответ — и так по кругу.

Проще говоря — своя Алиса, только self-hosted, без слива данных в облако и с подключением к любой LLM.

### Что умеет

| | |
|---|---|
| 🗣️ **Активация голосом** | Просыпается по ключевому слову (по умолчанию — *«киви»*) |
| 🎭 **Узнаёт по голосу** | Голосовые отпечатки через pyannote — различает людей |
| 🔐 **Разграничение доступа** | Владелец → Друг → Гость → Заблокирован. Опасные команды — через подтверждение в Telegram |
| 🔊 **Несколько движков озвучки** | ElevenLabs (облако), Piper (локально, бесплатно), Qwen3-TTS (GPU / RunPod) |
| ⚡ **Говорит сразу** | Разбивает ответ на предложения и начинает озвучку, не дожидаясь конца генерации |
| 🛑 **Можно перебить** | Начните говорить — ассистент замолчит и послушает |
| 🧠 **Запоминает голоса** | Новый голос автоматически сохраняется после первого общения |
| 🔌 **WebSocket** | Работает через OpenClaw Gateway v3 (потоковые delta/final события) |

## Как устроен

```
Микрофон → VAD + детекция энергии → Faster Whisper → Проверка ключевого слова
  → Определение говорящего (pyannote) → Проверка прав → Контроль безопасности
  → OpenClaw Gateway (WebSocket) → Потоковый ответ LLM
  → Озвучка по предложениям → Воспроизведение (с возможностью перебить)
  → Снова слушает
```

## Быстрый старт

### Что нужно

- **Python 3.10+**
- **FFmpeg**
- Работающий **[OpenClaw](https://github.com/openclaw/openclaw)**
- **GPU с CUDA** — желательно (для распознавания речи и локальной озвучки), но можно и без

### Установка

```bash
git clone https://github.com/ekleziast/kiwi-voice.git
cd kiwi-voice

python -m venv venv
# Linux / macOS
source venv/bin/activate
# Windows
venv\Scripts\activate

pip install -r requirements.txt
```

### Настройка

```bash
cp .env.example .env
# Впишите API-ключи (ElevenLabs, RunPod, Telegram — все необязательные)
```

Подправьте `config.yaml` под себя:

```yaml
# Движок озвучки: elevenlabs | piper | qwen3
tts:
  provider: "piper"          # Бесплатный, работает локально

# Распознавание речи
stt:
  model: "small"             # small = быстрее, large = точнее
  device: "cuda"             # cuda | cpu

# Ключевое слово
wake_word:
  keyword: "kiwi"

# Ваше имя (для команд вроде «запомни меня как ...»)
speaker_priority:
  owner:
    name: "Owner"            # Замените на своё
```

### Запуск

```bash
python -m kiwi
```

На Windows есть готовые лаунчеры:

```bash
start.bat
.\start.ps1
```

## Движки озвучки

| Движок | Качество | Задержка | Цена | Нужен GPU |
|--------|----------|----------|------|-----------|
| **ElevenLabs** | Отличное | ~1–2 сек | ~$0.30 / 1K символов | Нет |
| **Qwen3-TTS (локально)** | Высокое | ~1–3 сек | Бесплатно | Да (CUDA) |
| **Qwen3-TTS (RunPod)** | Высокое | ~2–5 сек | ~$0.0003 / сек | Нет |
| **Piper** | Хорошее | <0.5 сек | Бесплатно | Нет |

Сменить движок можно в `config.yaml` или переменной окружения:

```bash
KIWI_TTS_PROVIDER=piper python -m kiwi
```

## Контроль доступа

Kiwi узнаёт говорящего по голосу и решает, что ему можно:

```
ВЛАДЕЛЕЦ  (приоритет 0)  — Полный доступ, нельзя заблокировать
ДРУГ      (приоритет 1)  — Опасные команды — только с подтверждением в Telegram
ГОСТЬ     (приоритет 2)  — Любые чувствительные команды — через подтверждение
БЛОК      (приоритет 99) — Полностью игнорируется
```

### Голосовые команды

| Что сказать | Что произойдёт |
|-------------|----------------|
| *«Киви, запомни мой голос»* | Сохранит ваш голосовой отпечаток как владельца |
| *«Киви, это мой друг Саша»* | Добавит голос в категорию «Друг» |
| *«Киви, в чёрный список»* | Заблокирует последнего говорившего |
| *«Киви, кто говорит?»* | Скажет, кого узнал |
| *«Киви, какие голоса знаешь?»* | Перечислит всех запомненных |

### Подтверждение через Telegram

Если кто-то кроме владельца произносит потенциально опасную команду — Kiwi пришлёт уведомление в Telegram. Можно одобрить или отклонить прямо с телефона.

Для этого укажите `KIWI_TELEGRAM_BOT_TOKEN` и `KIWI_TELEGRAM_CHAT_ID` в `.env`.

## Переменные окружения

| Переменная | Зачем |
|------------|-------|
| `KIWI_ELEVENLABS_API_KEY` | Ключ ElevenLabs |
| `RUNPOD_API_KEY` | Ключ RunPod (Qwen3-TTS serverless) |
| `RUNPOD_TTS_ENDPOINT_ID` | ID эндпоинта RunPod |
| `KIWI_TELEGRAM_BOT_TOKEN` | Токен Telegram-бота |
| `KIWI_TELEGRAM_CHAT_ID` | Chat ID для уведомлений |
| `KIWI_TTS_PROVIDER` | Принудительный выбор движка озвучки |
| `KIWI_FFMPEG_PATH` | Путь к FFmpeg |
| `KIWI_DEBUG` | Подробные логи |
| `LLM_MODEL` | Модель LLM |

Полный список — в `.env.example`.

## Структура проекта

```
kiwi-voice/
├── kiwi/                    # Основной пакет
│   ├── service.py           # Главный оркестратор
│   ├── listener.py          # Микрофон, Whisper, wake word, VAD
│   ├── speaker_id.py        # Голосовые отпечатки (pyannote)
│   ├── speaker_manager.py   # Приоритеты говорящих + кэш
│   ├── voice_security.py    # Опасные команды + Telegram
│   ├── openclaw_ws.py       # WebSocket-клиент (Gateway v3)
│   ├── config_loader.py     # Загрузка конфигурации
│   ├── text_processing.py   # Разбивка текста на предложения
│   ├── unified_vad.py       # Детекция голосовой активности
│   ├── hardware_aec.py      # Подавление эха
│   ├── task_announcer.py    # Оповещения о долгих задачах
│   └── tts/                 # Движки озвучки
│       ├── elevenlabs.py
│       ├── piper.py
│       ├── qwen_local.py
│       ├── runpod.py
│       └── streaming.py     # Потоковая озвучка по предложениям
├── runpod/                  # Деплой на RunPod (Qwen3-TTS)
├── scripts/                 # Вспомогательные скрипты
├── sounds/                  # Звуки (старт, подтверждение, ожидание)
├── tests/                   # Тесты
├── config.yaml              # Конфигурация
├── .env.example             # Шаблон секретов
└── pyproject.toml           # Метаданные пакета
```

## Для разработчиков

```bash
# Тесты
pytest tests/

# Правила:
# - Логи: kiwi_log("TAG", "message", level="INFO"), не print()
# - Пути: PROJECT_ROOT из пакета kiwi
# - Опциональные зависимости: try/except + флаги *_AVAILABLE
# - Потоки: daemon threads + защита от падений
# - GPU: автодетекция CUDA, fallback на CPU
```

## Планы

- [ ] Английские голосовые команды
- [ ] Веб-интерфейс настройки
- [ ] Плагины для wake word
- [ ] Мультиязычное распознавание
- [ ] Интеграция с Home Assistant

## Лицензия

[MIT](LICENSE)

---

<p align="center">
  Сделано на 🥝 и массе кофеина
</p>
