<p align="center">
  <img src="https://em-content.zobj.net/source/apple/391/kiwi-fruit_1f95d.png" width="120" alt="Kiwi Voice">
</p>

<h1 align="center">Kiwi Voice</h1>

<p align="center">
  <strong>Голосовой ассистент для OpenClaw — идентификация говорящего, голосовая безопасность, прерывание ответа и потоковый TTS</strong>
</p>

<p align="center">
  <a href="https://github.com/ekleziast/kiwi-voice/blob/main/LICENSE"><img src="https://img.shields.io/badge/license-MIT-blue.svg" alt="License: MIT"></a>
  <a href="https://www.python.org/downloads/"><img src="https://img.shields.io/badge/python-3.10%2B-blue.svg" alt="Python 3.10+"></a>
  <a href="https://github.com/openclaw/openclaw"><img src="https://img.shields.io/badge/backend-OpenClaw-orange.svg" alt="OpenClaw"></a>
</p>

<p align="center">
  <a href="README.md">🇬🇧 English documentation</a>
</p>

---

## Что такое Kiwi Voice?

Kiwi Voice — голосовой интерфейс реального времени, превращающий [OpenClaw](https://github.com/openclaw/openclaw) в hands-free ассистента. Захватывает аудио с микрофона, распознаёт речь локально через Faster Whisper, идентифицирует *кто* говорит, применяет голосовые политики безопасности, общается с любой LLM через WebSocket-шлюз OpenClaw и озвучивает ответ — всё в непрерывном цикле.

По сути — самохостящийся Alexa/Siri с фокусом на приватность, подключённый к вашему AI-стеку.

### Ключевые возможности

| Возможность | Описание |
|-------------|----------|
| 🗣️ **Wake Word** | Активация по настраиваемому слову (по умолчанию: *«киви»*) |
| 🎭 **Speaker ID** | Распознавание по голосовому отпечатку через pyannote — знает, кто говорит |
| 🔐 **Голосовая безопасность** | Иерархия приоритетов (Owner → Friend → Guest → Blocked) с Telegram-подтверждением опасных команд |
| 🔊 **Мульти-TTS** | ElevenLabs (облако), Piper (локальный/бесплатный), Qwen3-TTS (локальный GPU / RunPod serverless) |
| ⚡ **Потоковый TTS** | Sentence-aware разбивка — начинает говорить до завершения ответа LLM |
| 🛑 **Barge-In** | Прерывание ассистента на полуслове |
| 🧠 **Автообучение** | Автоматически запоминает новые голоса после первого взаимодействия |
| 🔌 **WebSocket** | Нативный протокол OpenClaw Gateway v3 с delta/final стримингом |

## Архитектура

```
Микрофон → VAD + Energy Detection → Faster Whisper STT → Проверка Wake Word
  → Speaker ID (pyannote) → Проверка приоритета → Голосовая безопасность
  → OpenClaw Gateway (WebSocket) → Стрим ответа LLM
  → Sentence-aware TTS → Воспроизведение (с barge-in)
  → Обратно к прослушиванию
```

## Быстрый старт

### Требования

- **Python 3.10+**
- **FFmpeg** (для обработки аудио)
- **[OpenClaw](https://github.com/openclaw/openclaw)**, запущенный локально
- **GPU с CUDA** рекомендуется (для STT и локального TTS), но не обязательно

### Установка

```bash
git clone https://github.com/ekleziast/kiwi-voice.git
cd kiwi-voice

python -m venv venv
# Linux / macOS
source venv/bin/activate
# Windows
venv\Scripts\activate

pip install -r requirements.txt
```

### Настройка

```bash
cp .env.example .env
# Заполнить API-ключи (ElevenLabs, RunPod, Telegram — все опциональны)
```

Отредактировать `config.yaml` под своё окружение:

```yaml
# TTS-провайдер: elevenlabs | piper | qwen3
tts:
  provider: "piper"          # Бесплатный, локальный, без API-ключа

# Модель STT
stt:
  model: "small"             # small = быстро, large = точно
  device: "cuda"             # cuda | cpu

# Wake word
wake_word:
  keyword: "kiwi"

# Имя владельца (для голосовых команд «запомни меня как <имя>»)
speaker_priority:
  owner:
    name: "Owner"            # Измените на своё имя
```

### Запуск

```bash
python -m kiwi
```

Или через лаунчеры:

```bash
# Windows
start.bat
.\start.ps1

# Linux / macOS
python -m kiwi
```

## TTS-провайдеры

| Провайдер | Качество | Латентность | Стоимость | Локальный GPU |
|-----------|----------|-------------|-----------|---------------|
| **ElevenLabs** | Отличное | ~1–2 сек | ~$0.30/1K символов | Нет |
| **Qwen3-TTS (local)** | Высокое | ~1–3 сек | Бесплатно | Да (CUDA) |
| **Qwen3-TTS (RunPod)** | Высокое | ~2–5 сек | ~$0.0003/сек | Нет |
| **Piper** | Хорошее | <0.5 сек | Бесплатно | Нет |

Переключение провайдера в `config.yaml` или через переменную окружения:

```bash
KIWI_TTS_PROVIDER=piper python -m kiwi
```

## Голосовая безопасность

Kiwi идентифицирует говорящего по голосовому отпечатку и применяет иерархию приоритетов:

```
OWNER (приоритет 0)    — Полный доступ, не может быть заблокирован
FRIEND (приоритет 1)   — Опасные команды требуют Telegram-подтверждения
GUEST (приоритет 2)    — Все потенциально опасные команды требуют подтверждения
BLOCKED (приоритет 99) — Полный игнор
```

### Голосовые команды

| Команда | Действие |
|---------|----------|
| *«Киви, запомни мой голос»* | Зарегистрировать голосовой отпечаток как владелец |
| *«Киви, это мой друг [имя]»* | Добавить в категорию Friend |
| *«Киви, добавь в чёрный список»* | Заблокировать последнего говорящего |
| *«Киви, кто говорит?»* | Идентифицировать текущего говорящего |
| *«Киви, какие голоса ты знаешь?»* | Список всех запомненных голосов |

### Telegram-подтверждение

Когда не-владелец произносит потенциально опасную команду, Kiwi отправляет запрос на подтверждение владельцу в Telegram. Владелец может одобрить или отклонить с телефона.

Для активации задать `KIWI_TELEGRAM_BOT_TOKEN` и `KIWI_TELEGRAM_CHAT_ID` в `.env`.

## Переменные окружения

| Переменная | Описание |
|------------|----------|
| `KIWI_ELEVENLABS_API_KEY` | API-ключ ElevenLabs |
| `RUNPOD_API_KEY` | API-ключ RunPod (для Qwen3-TTS serverless) |
| `RUNPOD_TTS_ENDPOINT_ID` | ID эндпоинта RunPod |
| `KIWI_TELEGRAM_BOT_TOKEN` | Токен Telegram-бота (голосовая безопасность) |
| `KIWI_TELEGRAM_CHAT_ID` | Chat ID для сообщений подтверждения |
| `KIWI_TTS_PROVIDER` | Переопределить TTS-провайдер |
| `KIWI_FFMPEG_PATH` | Путь к FFmpeg |
| `KIWI_DEBUG` | Включить отладочное логирование |
| `LLM_MODEL` | Переопределить модель LLM |

Полный список — в `.env.example`.

## Структура проекта

```
kiwi-voice/
├── kiwi/                    # Основной Python-пакет
│   ├── service.py           # Главный оркестратор
│   ├── listener.py          # Захват аудио, Whisper STT, wake word, VAD
│   ├── speaker_id.py        # Извлечение голосовых отпечатков (pyannote)
│   ├── speaker_manager.py   # Иерархия приоритетов + hot cache
│   ├── voice_security.py    # Детекция опасных команд + Telegram approval
│   ├── openclaw_ws.py       # WebSocket-клиент (OpenClaw Gateway v3)
│   ├── config_loader.py     # Загрузка YAML/env конфигурации
│   ├── text_processing.py   # Очистка текста и разбивка на предложения для TTS
│   ├── unified_vad.py       # Voice Activity Detection
│   ├── hardware_aec.py      # Acoustic Echo Cancellation
│   ├── task_announcer.py    # Анонсер статуса длительных задач
│   └── tts/                 # TTS-провайдеры
│       ├── elevenlabs.py
│       ├── piper.py
│       ├── qwen_local.py
│       ├── runpod.py
│       └── streaming.py     # Sentence-aware streaming manager
├── runpod/                  # Деплой RunPod serverless (Qwen3-TTS)
├── scripts/                 # Утилиты
├── sounds/                  # Звуковые ассеты (старт, подтверждение, idle)
├── tests/                   # Smoke-тесты
├── config.yaml              # Основная конфигурация
├── .env.example             # Шаблон секретов
└── pyproject.toml           # Метаданные пакета
```

## Разработка

```bash
# Запуск тестов
pytest tests/

# Соглашения по коду:
# - Логирование: kiwi_log("TAG", "message", level="INFO") — никогда print()
# - Пути: PROJECT_ROOT из пакета kiwi
# - Опциональные модули: try/except + флаги *_AVAILABLE
# - Потоки: daemon threads + crash protection
# - GPU: автодетекция CUDA с fallback на CPU
```

## Roadmap

- [ ] Поддержка английских голосовых команд
- [ ] Веб-интерфейс для настройки
- [ ] Система плагинов для wake word
- [ ] Мультиязычный STT
- [ ] Интеграция с Home Assistant

## Лицензия

[MIT](LICENSE) — делайте что хотите.

---

<p align="center">
  Собрано на 🥝 и слишком большом количестве кофе
</p>
